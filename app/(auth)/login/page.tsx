/**
 * Route `/login` — page de connexion admin/diagnostiqueur.
 * Stub Sprint 0. Implémentation Supabase Auth (email+password, magic link)
 * au Sprint 1 (F-02).
 */
export default function LoginPage() {
  return (
    <div className="rounded-2xl border border-neutral-200 bg-white p-6 shadow-sm">
      <h1 className="text-xl font-semibold">Connexion</h1>
      <p className="mt-2 text-sm text-neutral-600">
        Authentification Supabase (email + mot de passe + magic link fallback)
        arrivera au Sprint 1. Cette page est un stub.
      </p>
      <div className="mt-4 font-mono text-[11px] tracking-widest text-neutral-400">
        SPRINT 0 — STUB
      </div>
    </div>
  );
}
